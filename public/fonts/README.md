Place custom handwriting font files in this folder.

Expected files:
- `PatrickHand-Regular.ttf`
- `InkifyPen-Regular.ttf`
- `InkifyMarker-Regular.ttf`

The app dynamically registers these files at runtime (if present) and falls back
to system/web fonts when a file is missing.
